# План разработки Telegram бота

> Техническое видение: @doc/vision.md | Правила кода: @doc/conventions.md

## 📊 Отчет по прогрессу

**Текущий статус:** В разработке - новая концепция сервиса 🚀  
**Завершенные итерации:** 16/18  
**Последнее обновление:** 20.01.2026  

### Статус итераций (старая концепция - консультант)
- [x] **Итерация 1:** Базовая инфраструктура - Завершено
- [x] **Итерация 2:** Интеграция с LLM - Завершено  
- [x] **Итерация 3:** Память диалогов - Завершено
- [x] **Итерация 4:** Системный промпт - Завершено
- [x] **Итерация 5:** Команды и обработка - Завершено
- [x] **Итерация 6:** Деплой и финализация - Завершено
- [x] **Итерация 7:** Облачный деплой на VPS - Завершено
- [x] **Итерация 8:** Управление системным промптом - Завершено

### Статус итераций (новая концепция - сочинение детских историй)
- [x] **Итерация 9:** Базовая механика создания историй + рефакторинг
- [x] **Итерация 10:** Процесс сочинения и завершение
- [x] **Итерация 11:** Уровень креативности и справочник жанров
- [x] **Итерация 12:** База данных для хранения историй
- [x] **Итерация 13:** Генерация персональной похвалы
- [x] **Итерация 14:** Удаление историй
- [x] **Итерация 15:** Голосовой ввод
- [x] **Итерация 16:** Генерация обложек
- [ ] **Итерация 17:** Загрузка изображений для вдохновения
- [ ] **Итерация 18:** Фильтрация контента  

---

## 🎯 Итерация 1: Базовая инфраструктура ✅
**Цель:** Запускаемый бот, отвечающий "Hello World"

- [x] Создать структуру проекта согласно @doc/vision.md раздел 3
- [x] Настроить `pyproject.toml` с зависимостями (aiogram, python-dotenv)
- [x] Создать `config.py` для загрузки переменных окружения
- [x] Реализовать `src/main.py` - точка входа с инициализацией бота
- [x] Создать `src/bot/handlers.py` с базовым обработчиком `/start`
- [x] Добавить `.env.example` с примером конфигурации
- [x] Создать `Makefile` с командами `install` и `run`

**Тест:** ✅ Инфраструктура готова, валидация работает

---

## 🎯 Итерация 2: Интеграция с LLM ✅
**Цель:** Бот отвечает через LLM на любое сообщение

- [x] Реализовать `src/llm/client.py` - асинхронный клиент для OpenRouter
- [x] Создать обработчик текстовых сообщений в `handlers.py`
- [x] Добавить базовое логирование запросов/ответов LLM
- [x] Добавить обработку ошибок LLM API

**Тест:** ✅ Бот отвечает через LLM на любое текстовое сообщение

---

## 🎯 Итерация 3: Память диалогов ✅
**Цель:** Бот помнит контекст беседы

- [x] Реализовать `src/storage/memory.py` - хранение сессий в памяти
- [x] Добавить сохранение истории сообщений по chat_id
- [x] Реализовать ограничение на количество сообщений в истории
- [x] Обновить обработчики для работы с контекстом

**Тест:** ✅ Бот помнит предыдущие сообщения в рамках диалога
**Дополнительно реализовано:** Команда `/clear` для очистки истории, Makefile команды `start/stop/status`

---

## 🎯 Итерация 4: Системный промпт ✅
**Цель:** Бот консультирует согласно своей роли

- [x] Создать `src/prompts/system.txt` с описанием роли ассистента
- [x] Реализовать `src/llm/prompts.py` - загрузка и форматирование промптов
- [x] Добавить системный промпт в каждый диалог
- [x] Настроить промпт на консультирование клиентов

**Тест:** ✅ Бот ведет себя как консультант специалиста, рассказывает о компетенциях и услугах

---

## 🎯 Итерация 5: Команды и обработка ✅
**Цель:** Полный набор команд и граничные случаи

- [x] Добавить команду `/help` с описанием возможностей
- [x] Реализовать обработку длинных сообщений
- [x] Добавить graceful обработку ошибок для пользователя
- [x] Улучшить логирование согласно @doc/conventions.md

**Тест:** ✅ Все команды реализованы, обработка ошибок улучшена, логирование структурировано

---

## 🎯 Итерация 6: Деплой и финализация ✅
**Цель:** Готовый к продакшн боту

- [x] Создать `Dockerfile` для контейнеризации
- [x] Добавить Make команды для Docker (`docker-build`, `docker-deploy`, `docker-stop`, `docker-logs`)
- [x] Написать базовые тесты с pytest
- [x] Обновить документацию с инструкциями по запуску (два варианта - через docker и без использования docker)
- [x] Провести финальное тестирование всех сценариев

**Тест:** ✅ Docker образ собран успешно (313MB), pytest тесты пройдены (3/3)
**Дополнительно реализовано:** `.dockerignore` для оптимизации образа, dev-зависимости в `pyproject.toml`

---

## 🎯 Итерация 7: Облачный деплой на VPS ✅
**Цель:** Создать инструкцию по развертыванию бота на облачном VPS с Docker

- [x] Подготовить команды для настройки SSH доступа к серверу
- [x] Подготовить команды для установки Docker и Docker Compose на Ubuntu
- [x] Подготовить команды для настройки файрвола и базовой безопасности
- [x] Подготовить команды для клонирования репозитория на VPS
- [x] Подготовить инструкцию по настройке переменных окружения (.env файл)
- [x] Подготовить команды для сборки и запуска Docker контейнера
- [x] Подготовить команды для настройки автозапуска бота (docker restart policy)
- [x] Создать `doc/guides/vps_deployment.md` с полной пошаговой инструкцией
- [x] Обновить `README.md` с информацией о деплое на VPS

**Тест:** ✅ Инструкция создана, включает все необходимые команды и разделы troubleshooting
**Примечание:** VPS на beget.com с Ubuntu Linux создается и настраивается вручную перед началом

---

## 🎯 Итерация 8: Управление системным промптом ✅
**Цель:** Расширить возможности бота по управлению системным промптом

- [x] Добавить команду `/prompt` для вывода текущего системного промпта
- [x] Добавить команду `/setprompt <новый_промпт>` для изменения системного промпта в текущей сессии
- [x] При выполнении `/setprompt` автоматически очищать историю сообщений
- [x] Обновить команду `/clear` для сброса системного промпта в изначальный вариант
- [x] Обновить `src/storage/memory.py` для хранения кастомного промпта сессии
- [x] Обновить `src/llm/prompts.py` для работы с динамическими промптами
- [x] Обновить команду `/help` с описанием новых команд
- [x] Добавить тесты для новых команд

**Тест:** ✅ Все 7 тестов пройдены (3 старых + 4 новых). Бот корректно показывает, изменяет и сбрасывает системный промпт

---

## 🎯 Итерация 9: Базовая механика создания историй + рефакторинг ✅
**Цель:** Создание истории с параметрами и начало сочинения, удаление нерелевантного функционала

**Задачи по удалению старого функционала:**
- [x] Удалить команду `/prompt` из `bot/handlers/commands.py`
- [x] Удалить команду `/setprompt` из `bot/handlers/commands.py`
- [x] Удалить связанные тесты для `/prompt` и `/setprompt`
- [x] Удалить или переработать команду `/clear` (пока удалить, позже добавим `/cancel`)
- [x] Переписать `src/prompts/system.txt` под роль помощника в сочинении детских историй
- [x] Упростить `storage/memory.py` - убрать хранение полной истории диалога

**Задачи по новому функционалу:**
- [x] Реализовать автоматическое приветствие для новых пользователей при первом сообщении
- [x] Создать `src/story/manager.py` - управление процессом сочинения
- [x] Добавить команду `/new_story` для создания истории
- [x] Реализовать выбор жанра через inline-кнопки (4 жанра: сказка, приключение, фэнтези, детектив)
- [x] Реализовать выбор длительности через inline-кнопки (короткая/средняя/длинная)
- [x] Реализовать ввод имени главного героя (текстовый ввод)
- [x] Реализовать выбор "кто начинает" через inline-кнопки (бот или пользователь)
- [x] Адаптировать `storage/memory.py` для хранения state активной истории
- [x] Начинать историю согласно выбранным параметрам
- [x] Обновить `/help` с описанием новой концепции
- [x] Создать `src/bot/keyboards.py` для inline-клавиатур

**Тест:** ✅ Старые команды `/prompt`, `/setprompt`, `/clear` удалены. Новый пользователь получает автоматическое приветствие при первом сообщении. Можно создать историю с параметрами через inline-кнопки, история начинается согласно выбору "кто начинает". Промпт соответствует роли помощника в сочинении историй. Все 7 тестов пройдены.

---

## 🎯 Итерация 10: Процесс сочинения и завершение ✅
**Цель:** Полный цикл сочинения от начала до финального текста

**Задачи:**
- [x] Реализовать чередование сообщений user/assistant в `story/manager.py`
- [x] Добавить подсчет пар сообщений
- [x] Реализовать определение приближения к концу истории
- [x] Добавить предложение завершить при достижении лимита пар
- [x] Реализовать возможность продолжить после предложения завершить (с увеличением лимита на +3)
- [x] Создать `src/story/formatter.py` - финализация истории
- [x] Реализовать генерацию названия истории через LLM
- [x] Реализовать компиляцию финального текста с форматированием (абзацы, прямая речь)
- [x] Обновить промпт `src/prompts/storytelling.txt` для процесса сочинения
- [x] Создать промпт `src/prompts/finalization.txt` для завершения
- [x] Добавить инструкции для подведения к концу (за 2 и 1 пару до лимита)
- [x] Реализовать динамический лимит с увеличением при продолжении

**Тест:** ✅ Можно сочинить историю до конца, получить предложение завершить, подтвердить и получить финальный текст с сгенерированным названием и красивым форматированием

---

## 🎯 Итерация 11: Уровень креативности и справочник жанров ✅
**Цель:** Расширенный выбор параметров и адаптация стиля

**Задачи:**
- [x] Создать `data/genres.json` со справочником жанров и референсами (5-7 жанров)
- [x] Реализовать загрузку и отображение справочника жанров при создании истории
- [x] Добавить ввод дополнительных персонажей (опционально) после ввода главного героя
- [x] Сохранять `additional_heroes` в параметрах истории в памяти
- [x] Добавить выбор уровня креативности (низкий/средний/высокий)
- [x] Сохранять `creativity_level` в параметрах истории в памяти
- [x] Переименовать `src/llm/` в `src/ai/` и `client.py` в `llm.py`
- [x] Реализовать использование температуры из параметров в `ai/llm.py`
- [x] Улучшить промпт для адаптации под стиль пользователя
- [x] Добавить параметры креативности в конфигурацию

**Тест:** ✅ При создании истории показывается справочник жанров с описаниями (5 жанров), можно ввести дополнительных персонажей (опционально), можно выбрать уровень креативности, бот использует соответствующую температуру, все параметры сохраняются в сессии, структура переименована в src/ai/

---

## 🎯 Итерация 12: База данных для хранения историй ✅
**Цель:** Постоянное хранение историй между перезапусками

**Задачи:**
- [x] Создать `src/storage/schema.sql` с таблицей stories (все поля согласно vision.md раздел 5)
- [x] Реализовать `src/storage/database.py` - CRUD операции с SQLite
- [x] Реализовать `src/storage/models.py` - структуры данных
- [x] Сохранять текущий прогресс истории в БД после каждого сообщения
- [x] Сохранять финальную историю в БД при завершении
- [x] Восстанавливать активную историю при перезапуске бота
- [x] Добавить команду `/my_stories` для просмотра списка историй
- [x] Реализовать просмотр сохраненной истории из списка
- [x] Добавить Make команду `make init-db` для создания таблиц
- [x] Обновить конфигурацию с параметром `db_path`

**Тест:** ✅ Истории сохраняются в БД, восстанавливаются при перезапуске, команда `/my_stories` показывает список историй (завершенных, заброшенных, активных) с возможностью просмотра. Все 15 тестов (7 старых + 8 новых) пройдены.

**Дополнительно реализовано:**
- Автоматическая пометка старых активных историй как `abandoned` при создании новой
- Уведомление пользователя о сохранении незавершенной истории
- Инициализация БД автоматически при старте бота
- Полный набор тестов для БД функциональности

---

## 🎯 Итерация 13: Генерация персональной похвалы ✅
**Цель:** Мотивация и поощрение ребенка после завершения истории

**Задачи:**
- [x] Создать функцию генерации персональной похвалы в `ai/llm.py`
- [x] Анализировать вклад пользователя в историю
- [x] Выделять лучшие идеи и креативные моменты ребенка
- [x] Добавить поле `praise_text` в финализацию истории
- [x] Сохранять похвалу в БД при завершении
- [x] Отправлять похвалу после финального текста и названия
- [x] Создать промпт `src/prompts/praise.txt` для генерации позитивной похвалы

**Тест:** ✅ При завершении истории бот генерирует и отправляет персональную похвалу с выделением лучших идей ребенка, похвала сохраняется в БД. Все 18 тестов (15 старых + 3 новых) пройдены.

**Реализовано:**
- Функция `generate_praise()` в `ai/llm.py` с температурой 0.75
- Промпт `src/prompts/praise.txt` для генерации похвалы
- Обновлена `database.complete_story()` для сохранения похвалы
- Обновлена `formatter.finalize_story()` для генерации похвалы
- Обновлен обработчик в `messages.py` для отправки похвалы отдельным сообщением
- Обновлена документация в `doc/vision.md`
- Созданы тесты в `tests/test_praise.py`

---

## 🎯 Итерация 14: Удаление историй ✅
**Цель:** Управление сохраненными историями

**Задачи:**
- [x] Добавить кнопку "Удалить" при просмотре истории из `/my_stories`
- [x] Реализовать запрос подтверждения удаления
- [x] Реализовать удаление истории из БД
- [x] Реализовать удаление связанных файлов (обложка, загруженное изображение)
- [x] Обновить клавиатуры в `src/bot/keyboards.py`
- [x] Логировать удаление историй

**Тест:** ✅ Можно удалить историю с подтверждением, история и связанные файлы удаляются из БД и файловой системы. Все 21 тест пройдены (18 старых + 3 новых).

**Реализовано:**
- Функции `get_story_actions_keyboard()` и `get_delete_confirmation_keyboard()` в `keyboards.py`
- Функция `delete_story()` в `database.py` с удалением файлов
- Обработчики `delete_story_request()`, `confirm_delete_story()`, `cancel_delete_story()`, `back_to_stories_callback()` в `commands.py`
- Параметр `images_base_path` в `config.py`
- Проверка владельца истории перед удалением (`story["user_id"] == chat_id`)
- Тесты в `tests/test_delete_story.py`

---

## 🎯 Итерация 15: Голосовой ввод ✅
**Цель:** Возможность сочинять историю голосом

**Задачи:**
- [x] Создать `src/ai/speech.py` - интеграция с Speech-to-Text API
- [x] Добавить обработку голосовых сообщений в `bot/handlers/messages.py`
- [x] Реализовать конвертацию голоса в текст через Speech API
- [x] Добавить параметры Speech API в конфигурацию
- [x] Логировать запросы к Speech API
- [x] Обработать ошибки распознавания голоса gracefully
- [x] Обновить `/help` с информацией о голосовом вводе

**Тест:** ✅ Можно отправить голосовое сообщение в процессе сочинения, бот распознает его и использует как часть истории

**Реализовано:**
- Модуль `src/ai/speech.py` с функцией `transcribe_voice()` для распознавания голоса
- Обработчик `voice_handler()` с фильтром `F.voice` для голосовых сообщений
- Временные файлы с автоматическим удалением после обработки
- Graceful error handling с понятными сообщениями для пользователя
- Логирование запросов/ответов Speech API с размером файла и длиной текста
- Переиспользование существующей логики через MockMessage
- Параметры Speech API в конфигурации (api_key, base_url, model)
- Обновлена команда `/help` с информацией о голосовом вводе
- Валидация наличия SPEECH_API_KEY при старте бота

---

## 🎯 Итерация 16: Генерация обложек ✅
**Цель:** Создание красивых обложек для историй

**Задачи:**
- [x] Создать `src/ai/image_gen.py` - интеграция с Image Generation API
- [x] Создать промпт `src/prompts/cover_generation.txt` для генерации обложек
- [x] Реализовать генерацию обложки при завершении истории
- [x] Формировать промпт для обложки с учетом жанра, героев и референсов
- [x] Сохранять обложку в `data/images/covers/{story_id}.png`
- [x] Сохранять путь к обложке в БД (`cover_url`)
- [x] Отправлять обложку вместе с финальным текстом
- [x] Добавить параметры Image Gen API в конфигурацию
- [x] Реализовать graceful fallback при ошибке генерации

**Тест:** ✅ При завершении истории бот генерирует обложку через polza.ai (seedream-v4) с учетом жанра, названия, автора и содержания. Обложка включает название истории и имя автора на изображении. При ошибке генерации - история завершается без обложки (graceful fallback)

**Реализовано:**
- Модуль `src/ai/image_gen.py` с асинхронной генерацией через polza.ai API
- Промпт включает: название (на обложке), автор (на обложке), жанр, главный герой, референсы, краткое содержание (200 символов)
- Polling механизм: 60 попыток × 3 сек = 180 сек (3 минуты)
- Формат обложки: 4:3 (вертикальная), качество 2K
- Добавлено поле `author_name` в БД для хранения имени автора из Telegram
- Автоматическая миграция БД для существующих таблиц
- Обновлен `pyproject.toml` с зависимостью `httpx`
- Добавлена универсальная функция `load_prompt()` в `src/ai/prompts.py`

---

## 🎯 Итерация 17: Загрузка изображений для вдохновения
**Цель:** Использование изображений для задания темы/стиля

**Задачи:**
- [ ] Создать `src/ai/vision.py` - интеграция с Vision API
- [ ] Добавить возможность загрузить изображение при создании истории
- [ ] Создать папки `data/images/uploads/` и `data/images/covers/`
- [ ] Реализовать сохранение загруженного изображения
- [ ] Реализовать анализ изображения через Vision API
- [ ] Использовать описание изображения в промпте для стиля истории
- [ ] Добавить валидацию размера и формата изображений
- [ ] Добавить поле `initial_image_url` в сохранение истории
- [ ] Добавить параметры Vision API в конфигурацию

**Тест:** Можно загрузить изображение при создании истории, бот анализирует его и адаптирует стиль повествования под изображение

---

## 🎯 Итерация 18: Фильтрация контента
**Цель:** Безопасность контента для детской аудитории

**Задачи:**
- [ ] Добавить проверку контента через moderation API или настройки LLM
- [ ] Реализовать фильтрацию сексуального контента в сообщениях пользователя
- [ ] Реализовать фильтрацию в генерируемых ботом сообщениях
- [ ] Добавить вежливое сообщение при обнаружении неподходящего контента
- [ ] Логировать попытки неподходящего контента (без содержимого)
- [ ] Обновить системный промпт с инструкциями по безопасности контента
- [ ] Добавить тесты для фильтрации контента

**Тест:** Бот блокирует неподходящий контент от пользователя и вежливо просит переформулировать, сам не генерирует неподходящий контент

---

## 📝 Примечания

- После каждой итерации обновлять раздел "Отчет по прогрессу"
- Тестировать каждую итерацию перед переходом к следующей
- В случае проблем - упрощать, следовать принципу KISS
- Документировать найденные проблемы и решения
- Следовать структуре из `@doc/vision.md` раздел 3
- Соблюдать правила из `@doc/conventions.md`
- Каждая итерация должна давать **работающий продукт**